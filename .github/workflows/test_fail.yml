name: Test Failure Pipeline
on: [push]

jobs:
  break_build:
    runs-on: ubuntu-latest
    steps:
      - name: Run a failing script
        run: |
          echo "Compiling code..."
          echo "Error: missing required module 'x'"
          exit 1